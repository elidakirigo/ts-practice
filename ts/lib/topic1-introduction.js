"use strict";
/**
 * ----------------------------
 * installation of typescript
 * ----------------------------
 *
/ typescript is used in large companies such as winJS in windows, Angular by google and microsoft ie VScode IntelliSence.
*/
/**
 * ---------------------------------
 * installing typescript globally
 * ---------------------------------
 *
 * using npm fow windows - npm install -g typescript@next or
 *                       - npm install -g typescript@[version]
 *
 * using typescript compiler on node using npm
 * run 'tsc [typescript file name]' to compile your .ts to .js
 *
 * watching = tsc -w [file name] or tsc --watch [file name]
 *
 * creating config files : tsc --init
 *
 * checking for a config file location : tsc -p
 *
 * ceating a typescript file : touch [file name.ts] note : without the extention it does not have any
 * ----------------------
 * the END
 * ----------------------
 */
//# sourceMappingURL=topic1-introduction.js.map