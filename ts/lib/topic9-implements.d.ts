import { Starks } from "./topic3-interfaces";
export declare class AutoStart implements Starks {
    figure: boolean;
    contactDetails?: number;
    constructor();
    initials(figure: boolean): void;
}
